# Ephemeral MsSql DB Context

Python library to allow easy coding of databases integration tests.

## PyPi Package

The package is available at PyPi at TBD

## Quickstart

To use the library first we install it by using PyPi:

```shell
pip install ephemerals-mssql
```

Then we can include the package on our test classes and start using it. Let's say we need to test some queries to our database:

```python
```

In the code shown above:

- TBD

When the ephemeral DB context is disposed the created database will be DROPPED.
